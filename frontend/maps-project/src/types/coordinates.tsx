/**
 * This interface contains data for lat / long coordinates on the map.
 */
interface LatLng {
  lattitude: number;
  longitude: number;
}

/**
 *
 */
interface Point {
  x: number;
  y: number;
}
